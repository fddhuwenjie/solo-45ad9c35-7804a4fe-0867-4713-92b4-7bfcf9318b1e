"""公共层：阀门档案、仪器校准链与时序处理。

- ``db``：单连接存储核心（阀门档案、校准版本）与共享建表/解码工具；
- ``schemas``：跨诊断能力共享的请求模型（序列、量程、不确定度、校准版本等）；
- ``valves`` / ``calversions`` / ``samples``：公共 APIRouter；
- ``processing``：时序处理（单位统一、时间对齐、分段、指标、异常检测）；
- ``calibration``：逐通道校准链领域逻辑（证书版本、修正、绑定解析）。

公共层不得依赖任何诊断能力包，也不得依赖装配层（app.main/app.registry/app.db）。
"""
